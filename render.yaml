services:
  - type: web
    name: roshni-backend
    root_dir: ./backend
    env: python

    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -w 1 -k uvicorn.workers.UvicornWorker main:app

    envVars:
      # ✅ FORCE PYTHON VERSION (CRITICAL FIX)
      - key: PYTHON_VERSION
        value: 3.11.9

      # ============ SYSTEM ============
      - key: ENVIRONMENT
        value: production
      - key: DEBUG
        value: "False"
      - key: LOG_LEVEL
        value: INFO

      # ============ URLS ============
      - key: BACKEND_URL
        sync: false
      - key: FRONTEND_URL
        sync: false
      - key: ALLOWED_HOSTS
        value: "*"

      # ============ DATABASE ============
      - key: DATABASE_URL
        sync: false

      # ============ ALGORAND ============
      - key: ALGORAND_NETWORK
        value: testnet
      - key: ALGORAND_NODE_URL
        value: https://testnet-api.algonode.cloud
      - key: ALGORAND_INDEXER_URL
        value: https://testnet-idx.algonode.cloud
      - key: ALGORAND_ADMIN_MNEMONIC
        sync: false
      - key: ALGORAND_ADMIN_PRIVATE_KEY
        sync: false
      - key: SUN_ASA_ID
        value: "756341116"

      # ============ AI / APIs ============
      - key: GEMINI_API_KEY
        sync: false

      # ============ DISCOM ============
      - key: DISCOM_FIXED_CHARGE
        value: "150.0"
      - key: DISCOM_ADMIN_FEE_PERCENT
        value: "3.0"
      - key: DISCOM_EXPORT_RATE
        value: "3.50"
      - key: DISCOM_GRID_RATE
        value: "7.50"

      # ============ SOLAR ============
      - key: SOLAR_EXPORT_RATE
        value: "5.80"
      - key: SOLAR_POOL_RATE
        value: "6.50"

      # ============ IoT ============
      - key: IOT_AUTH_TOKEN
        sync: false

  - type: web
    name: roshni-frontend
    root_dir: ./frontend
    env: node
    nodeVersion: 18
    buildCommand: npm ci && npm run build
    startCommand: npx serve -s dist -l 3000
    envVars:
      - key: VITE_BACKEND_URL
        value: https://roshni-backend-o7al.onrender.com
